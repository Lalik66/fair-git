# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
.next/
out/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
.nyc_output/

# Temporary files
tmp/
temp/
*.tmp

# Prisma
backend/prisma/*.db
backend/prisma/*.db-journal

# TypeScript
*.tsbuildinfo

# Misc
.cache/
.parcel-cache/
.eslintcache

# Debug
debug.log

# SQLite databases (except features.db which is for the feature tracker)
*.db
!features.db

# Cloudinary uploads (local copies)
uploads/

# Package lock files (optional - uncomment if using yarn/pnpm)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Claude Code worktrees (each contains its own .git pointer)
.claude/worktrees/
# Per-user Claude Code settings
.claude/settings.local.json

# MCP server config (contains API keys — keep local)
.mcp.json

# Deploy-time media content — hosted on Cloudinary, not in git (see DESIGN.md)
frontend/public/Charityfairmp4.mp4
frontend/public/Chertovo_koleso.webm
frontend/public/svetlee_background.mp4
frontend/public/fevvareler4.jpg

# Playwright MCP scratch output (screenshots/traces) — not source
.playwright-mcp/

# Ad-hoc test image fixtures — large binaries, not source
test-images/

# Windows stray artifact from redirecting command output to `nul`
nul
**/nul
